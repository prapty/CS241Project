main
var a;

function square(a);
{
    return a * a;
};

function pow(a, b);
var pow;
{
    let pow <- 1;
    while b > 0 do
        let pow <- pow * a;
        let b <- b - 1;
    od;
    let b <- b + 1;
    return pow;
    call OutputNum(pow)
};

function printZero();
{
    call OutputNum(0);
    return 0
};

{
     let a <- call pow(2, -call printZero)
}.