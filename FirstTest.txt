main
var a, b, c, d;
void function test (x, y);
var z;
{
  let z <- x + y;
  call OutputNum(z);
};
{
	let a <- call InputNum;

	let b <- a + 1;
    call OutputNum(b);
	while a + 1 != b
	do
		let a <- a + 1;
		let c <- a + 1;
		let d <- a + 1;
	od;

	call OutputNum(a);
	call OutputNum(b);
}.