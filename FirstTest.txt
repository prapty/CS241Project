main
var a, b, c;
function test (x, y);
var z;
{
  let z <- x + y;
  call OutputNum(z);
  return z;
};
{
	let a <- call InputNum;

	let b <- a + 1;

    let c <- call test(a+b, a*b);
	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.